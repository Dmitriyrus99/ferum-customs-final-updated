#####################################################################
# requirements.txt  – Ferum Customizations
#
# Этот файл ставится **в дополнение** к фреймворку Frappe/ERPNext,
# который Bench подтянет из репозиториев version-15. Здесь только
# то, что реально нужно самому кастом-приложению (интеграции,
# тесты, линтеры). При желании разделите на prod / dev.
#####################################################################

############################
# ── core runtime deps ──  #
############################
# Google Workspace / Sheets интеграции
google-api-python-client>=2.12.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.0.0
gspread>=6.0.0
oauth2client>=4.1.3

# Общие утилиты
requests>=2.31.0
python-dateutil>=2.8.2
pytz>=2024.1
redis>=4.6.0              # если обращаетесь к Redis напрямую
psycopg2-binary>=2.9.10    # нужен, если production-БД = PostgreSQL

#################################
# ── dev / CI / тест-зависимости ── #
#################################
pytest>=8.1.1
pytest-cov>=5.0.0
ruff>=0.4.4               # быстрый линтер
flake8>=6.1.0             # дополнительный стиль-чек

#####################################################################
# Заметка:
# * Frappe и ERPNext подтягиваются самим Bench (`bench init …`),
#   поэтому здесь их НЕ фиксируем.
# * Если вы разворачиваете вне Bench (чистый pip install), добавьте:
#
# frappe @ git+https://github.com/frappe/frappe@version-15
# erpnext @ git+https://github.com/frappe/erpnext@version-15
#
#####################################################################
